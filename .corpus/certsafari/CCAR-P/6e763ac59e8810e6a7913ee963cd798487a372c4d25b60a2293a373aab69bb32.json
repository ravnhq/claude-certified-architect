{
  "exam": "CCAR-P",
  "source": "https://www.certsafari.com/anthropic/quiz/claude-architect-professional",
  "question": "A support-triage agent invokes Claude roughly every 20 minutes with an unchanged 6,000-token system prompt and tool set, but the gaps between calls sometimes stretch past 10 minutes during quiet periods. Using the default ephemeral cache_control (5-minute TTL), the team keeps paying cache-write rates on many calls because the cache keeps expiring between calls. Which change most directly reduces this while keeping the cache reusable?",
  "options": [
    "A) Add a second automatic-caching breakpoint at the very start of the messages array, so the system prompt is cached as a separate entry with its own 5-minute TTL, letting it be reused on calls even if the tools cache expires.",
    "B) Set cache_control to {\"type\": \"ephemeral\", \"ttl\": \"1h\"} on the stable prefix so the cache entry survives the longer gaps between calls, at a higher per-write price than the 5-minute default.",
    "C) Remove cache_control entirely and send uncached requests, so each call avoids paying a cache-write fee for a 5-minute TTL that expires during quiet gaps over 10 minutes, paying only standard input token costs.",
    "D) Lower max_tokens to 0 on every triage call so each request pays only for cache reads and avoids output generation, offsetting the cost of the 5-minute cache write with savings on completion tokens."
  ],
  "correct_answers": [
    "B) Set cache_control to {\"type\": \"ephemeral\", \"ttl\": \"1h\"} on the stable prefix so the cache entry survives the longer gaps between calls, at a higher per-write price than the 5-minute default."
  ],
  "explanations": [
    "Incorrect. Adding a breakpoint does not give each part its own TTL; the entire cache entry expires after the same 5-minute period. The system prompt and tools would still be cached together (or the breakpoint might split them), but the TTL is per cache entry, not per segment, so this does not extend reuse time.",
    "Correct. Setting a 1-hour TTL keeps the cache entry alive across gaps longer than 5 minutes, so it won't expire between calls. Although the per-write cost is higher than the 5-minute default, it reduces the number of cache writes, making it cost-effective when calls are frequent enough to benefit from reuse but not always within 5 minutes of each other.",
    "Incorrect. Removing cache_control avoids cache-write fees but forces every call to pay full standard input token costs for the system prompt and tools. Even with occasional cache expirations, keeping caching with a longer TTL yields lower total cost by allowing cheap cache reads on most calls.",
    "Incorrect. Setting max_tokens=0 prevents the model from generating any output, making it useless for a support-triage agent. It does not extend the cache lifetime or avoid cache writes; it only eliminates output token costs, which doesn't address the cache expiry problem."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: CertSafari — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Architect – Professional (CCAR-P)\n- button \"13 / 35 correct (37%) View by domain\":\n  - generic: 13 / 35 correct (37%)\n  - generic: View by domain\n- progressbar\n- generic: Question 36 of 60\n- generic: \"Subdomain 2.5: Implement prompt reuse strategies\"\n- heading \"A support-triage agent invokes Claude roughly every 20 minutes with an unchanged 6,000-token system prompt and tool set, but the gaps between calls sometimes stretch past 10 minutes during quiet periods. Using the default ephemeral cache_control (5-minute TTL), the team keeps paying cache-write rates on many calls because the cache keeps expiring between calls. Which change most directly reduces this while keeping the cache reusable?\" [level=3]\n- paragraph: Incorrect\n- paragraph: Review the explanations to understand the correct answer.\n- radiogroup:\n  - radio \"A) Add a second automatic-caching breakpoint at the very start of the messages array, so the system prompt is cached as a separate entry with its own 5-minute TTL, letting it be reused on calls even if the tools cache expires.\" [checked] [disabled]:\n  - generic: A) Add a second automatic-caching breakpoint at the very start of the messages array, so the system prompt is cached as a separate entry with its own 5-minute TTL, letting it be reused on calls even if the tools cache expires.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Adding a breakpoint does not give each part its own TTL; the entire cache entry expires after the same 5-minute period. The system prompt and tools would still be cached together (or the breakpoint might split them), but the TTL is per cache entry, not per segment, so this does not extend reuse time.\n  - 'radio \"B) Set cache_control to {\\\"type\\\": \\\"ephemeral\\\", \\\"ttl\\\": \\\"1h\\\"} on the stable prefix so the cache entry survives the longer gaps between calls, at a higher per-write price than the 5-minute default.\" [disabled]'\n  - generic: \"B) Set cache_control to {\\\"type\\\": \\\"ephemeral\\\", \\\"ttl\\\": \\\"1h\\\"} on the stable prefix so the cache entry survives the longer gaps between calls, at a higher per-write price than the 5-minute default.\"\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Correct. Setting a 1-hour TTL keeps the cache entry alive across gaps longer than 5 minutes, so it won't expire between calls. Although the per-write cost is higher than the 5-minute default, it reduces the number of cache writes, making it cost-effective when calls are frequent enough to benefit from reuse but not always within 5 minutes of each other.\n  - radio \"C) Remove cache_control entirely and send uncached requests, so each call avoids paying a cache-write fee for a 5-minute TTL that expires during quiet gaps over 10 minutes, paying only standard input token costs.\" [disabled]\n  - generic: C) Remove cache_control entirely and send uncached requests, so each call avoids paying a cache-write fee for a 5-minute TTL that expires during quiet gaps over 10 minutes, paying only standard input token costs.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Removing cache_control avoids cache-write fees but forces every call to pay full standard input token costs for the system prompt and tools. Even with occasional cache expirations, keeping caching with a longer TTL yields lower total cost by allowing cheap cache reads on most calls.\n  - radio \"D) Lower max_tokens to 0 on every triage call so each request pays only for cache reads and avoids output generation, offsetting the cost of the 5-minute cache write with savings on completion tokens.\" [disabled]\n  - generic: D) Lower max_tokens to 0 on every triage call so each request pays only for cache reads and avoids output generation, offsetting the cost of the 5-minute cache write with savings on completion tokens.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded] [active]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Setting max_tokens=0 prevents the model from generating any output, making it useless for a support-triage agent. It does not extend the cache lifetime or avoid cache writes; it only eliminates output token costs, which doesn't address the cache expiry problem.\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:32:37.804Z",
  "fingerprint": "6e763ac59e8810e6a7913ee963cd798487a372c4d25b60a2293a373aab69bb32"
}
