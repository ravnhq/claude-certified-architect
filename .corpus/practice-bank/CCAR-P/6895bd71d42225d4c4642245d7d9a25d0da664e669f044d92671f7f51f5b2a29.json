{
  "exam": "CCAR-P",
  "question": "A CISO reviewing an Agent SDK rollout learns that permission_mode is set to bypassPermissions in a batch-processing pipeline and asks which authorization controls, if any, still constrain the agent under that mode. Based on how bypassPermissions is evaluated, which of the following statements are accurate? (Select 2)",
  "options": [
    "A) Hooks still execute and a hook that denies a call is still enforced, because hooks are evaluated before the permission mode step in the flow",
    "B) The plan permission mode and bypassPermissions produce identical behavior for file-edit tools, since both ultimately approve or deny edits at the same evaluation step",
    "C) allowed_tools still meaningfully restricts which tools are auto-approved, so listing only a narrow set of tools there keeps bypassPermissions scoped to just those tools",
    "D) An explicit ask rule configured in settings.json still forces the call through the canUseTool callback, even though the mode would otherwise auto-approve it",
    "E) disallowed_tools entries for whole tool names have no effect once bypassPermissions is active, since the mode is designed to override every other layer of the permission system"
  ],
  "correct_answers": [
    "A) Hooks still execute and a hook that denies a call is still enforced, because hooks are evaluated before the permission mode step in the flow",
    "D) An explicit ask rule configured in settings.json still forces the call through the canUseTool callback, even though the mode would otherwise auto-approve it"
  ],
  "explanations": [
    "Correct. Hooks run first in the evaluation order, ahead of deny rules, ask rules, and the permission mode step, so a hook denial still blocks the call even under bypassPermissions.",
    "plan mode explicitly routes file-edit and shell-write tools to canUseTool regardless of allow rules, while bypassPermissions auto-approves those same operations at the mode step; the two modes produce opposite outcomes for edits, not identical ones.",
    "allowed_tools only pre-approves the tools it lists; unlisted tools still fall through to the permission mode, where bypassPermissions approves them too. Listing a narrow set in allowed_tools does not scope or limit what bypassPermissions itself approves.",
    "Correct. Explicit ask rules are checked before the permission mode step, so a matching ask rule still routes the call to canUseTool for confirmation even when bypassPermissions would otherwise auto-approve it.",
    "disallowed_tools entries are deny rules, and deny rules are evaluated before the permission mode step and still block matching calls even in bypassPermissions mode; they are not overridden by it."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: the practice bank — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Architect – Professional (CCAR-P)\n- button \"3 / 9 correct (33%) View by domain\":\n  - generic: 3 / 9 correct (33%)\n  - generic: View by domain\n- progressbar\n- generic: Question 10 of 60\n- generic: \"Subdomain 3.2: Analyze authentication and authorization requirements to identify security gaps\"\n- heading \"A CISO reviewing an Agent SDK rollout learns that permission_mode is set to bypassPermissions in a batch-processing pipeline and asks which authorization controls, if any, still constrain the agent under that mode. Based on how bypassPermissions is evaluated, which of the following statements are accurate? (Select 2)\" [level=3]\n- paragraph: Incorrect\n- paragraph: Review the explanations to understand the correct answer.\n- checkbox \"A) Hooks still execute and a hook that denies a call is still enforced, because hooks are evaluated before the permission mode step in the flow\" [checked] [disabled]:\n- generic: A) Hooks still execute and a hook that denies a call is still enforced, because hooks are evaluated before the permission mode step in the flow\n- heading \"Explanation\" [level=3]:\n  - button \"Explanation\" [expanded]:\n    - generic: Explanation\n- region \"Explanation\":\n  - generic: Correct. Hooks run first in the evaluation order, ahead of deny rules, ask rules, and the permission mode step, so a hook denial still blocks the call even under bypassPermissions.\n- checkbox \"B) The plan permission mode and bypassPermissions produce identical behavior for file-edit tools, since both ultimately approve or deny edits at the same evaluation step\" [checked] [disabled]:\n- generic: B) The plan permission mode and bypassPermissions produce identical behavior for file-edit tools, since both ultimately approve or deny edits at the same evaluation step\n- heading \"Explanation\" [level=3]:\n  - button \"Explanation\" [expanded]:\n    - generic: Explanation\n- region \"Explanation\":\n  - generic: plan mode explicitly routes file-edit and shell-write tools to canUseTool regardless of allow rules, while bypassPermissions auto-approves those same operations at the mode step; the two modes produce opposite outcomes for edits, not identical ones.\n- checkbox \"C) allowed_tools still meaningfully restricts which tools are auto-approved, so listing only a narrow set of tools there keeps bypassPermissions scoped to just those tools\" [disabled]\n- generic: C) allowed_tools still meaningfully restricts which tools are auto-approved, so listing only a narrow set of tools there keeps bypassPermissions scoped to just those tools\n- heading \"Explanation\" [level=3]:\n  - button \"Explanation\" [expanded]:\n    - generic: Explanation\n- region \"Explanation\":\n  - generic: allowed_tools only pre-approves the tools it lists; unlisted tools still fall through to the permission mode, where bypassPermissions approves them too. Listing a narrow set in allowed_tools does not scope or limit what bypassPermissions itself approves.\n- checkbox \"D) An explicit ask rule configured in settings.json still forces the call through the canUseTool callback, even though the mode would otherwise auto-approve it\" [disabled]\n- generic: D) An explicit ask rule configured in settings.json still forces the call through the canUseTool callback, even though the mode would otherwise auto-approve it\n- heading \"Explanation\" [level=3]:\n  - button \"Explanation\" [expanded]:\n    - generic: Explanation\n- region \"Explanation\":\n  - generic: Correct. Explicit ask rules are checked before the permission mode step, so a matching ask rule still routes the call to canUseTool for confirmation even when bypassPermissions would otherwise auto-approve it.\n- checkbox \"E) disallowed_tools entries for whole tool names have no effect once bypassPermissions is active, since the mode is designed to override every other layer of the permission system\" [disabled]\n- generic: E) disallowed_tools entries for whole tool names have no effect once bypassPermissions is active, since the mode is designed to override every other layer of the permission system\n- heading \"Explanation\" [level=3]:\n  - button \"Explanation\" [expanded] [active]:\n    - generic: Explanation\n- region \"Explanation\":\n  - generic: disallowed_tools entries are deny rules, and deny rules are evaluated before the permission mode step and still block matching calls even in bypassPermissions mode; they are not overridden by it.\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:19:27.632Z",
  "fingerprint": "6895bd71d42225d4c4642245d7d9a25d0da664e669f044d92671f7f51f5b2a29"
}
