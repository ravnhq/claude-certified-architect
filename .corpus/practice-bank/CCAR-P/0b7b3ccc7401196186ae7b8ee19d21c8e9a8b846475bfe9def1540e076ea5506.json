{
  "exam": "CCAR-P",
  "question": "A regulated enterprise routes all Claude API traffic through a self-hosted proxy and sets ANTHROPIC_AUTH_TOKEN so the proxy authenticates requests with a bearer token it can rotate independently of Anthropic API keys. A developer, troubleshooting an unrelated issue, exports ANTHROPIC_API_KEY in their local shell profile and forgets to unset it. What is the authorization consequence the next time that developer runs Claude Code, and how should the team close the gap?",
  "options": [
    "A) ANTHROPIC_AUTH_TOKEN outranks ANTHROPIC_API_KEY in credential precedence, so the request still goes through the governed proxy and no authorization gap exists; the team should enforce proxy routing with an org-wide managed setting rather than relying on individual shell discipline.",
    "B) ANTHROPIC_API_KEY is checked after ANTHROPIC_AUTH_TOKEN in precedence, so the proxy uses the bearer token and the API key is ignored; the team should add a shell check to unset ANTHROPIC_API_KEY when ANTHROPIC_AUTH_TOKEN is present.",
    "C) Both variables get merged into a single request, sending the API key as an extra header beside the bearer token, so the proxy ignores it and authorization remains unchanged; the team can verify this by checking the proxy logs for only the expected bearer token.",
    "D) ANTHROPIC_API_KEY ranks above ANTHROPIC_AUTH_TOKEN, so the session bypasses the gateway proxy's rotation and audit controls entirely; enforce the gateway path with an org-wide managed setting instead of developer discipline."
  ],
  "correct_answers": [
    "A) ANTHROPIC_AUTH_TOKEN outranks ANTHROPIC_API_KEY in credential precedence, so the request still goes through the governed proxy and no authorization gap exists; the team should enforce proxy routing with an org-wide managed setting rather than relying on individual shell discipline."
  ],
  "explanations": [
    "Correct. Official Claude Code credential precedence lists cloud provider credentials first, then ANTHROPIC_AUTH_TOKEN, then ANTHROPIC_API_KEY. Because ANTHROPIC_AUTH_TOKEN outranks ANTHROPIC_API_KEY, the bearer token is used and the developer's key is ignored, so traffic still goes through the governed proxy. Best practice is to enforce the proxy path with an org-wide managed setting rather than relying on individual shell hygiene; this prevents accidental bypass if ANTHROPIC_AUTH_TOKEN is ever unset.",
    "Incorrect. The precedence fact is correct—ANTHROPIC_API_KEY is checked after ANTHROPIC_AUTH_TOKEN—but adding a shell check to unset the API key when the token is present is unnecessary and fragile. Because ANTHROPIC_AUTH_TOKEN already outranks ANTHROPIC_API_KEY, the key is ignored. Enterprise controls should be enforced through org-wide managed settings, not individual shell hygiene.",
    "Incorrect. Anthropic does not merge both credentials into one request. Claude Code uses credential precedence to select a single credential. When ANTHROPIC_AUTH_TOKEN is present, it is used and ANTHROPIC_API_KEY is ignored.",
    "Incorrect. Official authentication precedence in Claude Code places ANTHROPIC_AUTH_TOKEN above ANTHROPIC_API_KEY, not the reverse. An org-wide managed setting is a reasonable remediation, but the stated consequence of gateway bypass is false because the bearer token takes precedence."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: the practice bank — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Architect – Professional (CCAR-P)\n- button \"7 / 35 correct (20%) View by domain\":\n  - generic: 7 / 35 correct (20%)\n  - generic: View by domain\n- progressbar\n- generic: Question 36 of 60\n- generic: \"Subdomain 3.2: Analyze authentication and authorization requirements to identify security gaps\"\n- heading \"A regulated enterprise routes all Claude API traffic through a self-hosted proxy and sets ANTHROPIC_AUTH_TOKEN so the proxy authenticates requests with a bearer token it can rotate independently of Anthropic API keys. A developer, troubleshooting an unrelated issue, exports ANTHROPIC_API_KEY in their local shell profile and forgets to unset it. What is the authorization consequence the next time that developer runs Claude Code, and how should the team close the gap?\" [level=3]:\n  - text: A regulated enterprise routes all Claude API traffic through a self-hosted proxy and sets\n  - code: ANTHROPIC_AUTH_TOKEN\n  - text: so the proxy authenticates requests with a bearer token it can rotate independently of Anthropic API keys. A developer, troubleshooting an unrelated issue, exports\n  - code: ANTHROPIC_API_KEY\n  - text: in their local shell profile and forgets to unset it. What is the authorization consequence the next time that developer runs Claude Code, and how should the team close the gap?\n- paragraph: Correct!\n- paragraph: Great job! Review the explanations below.\n- radiogroup:\n  - radio \"A) ANTHROPIC_AUTH_TOKEN outranks ANTHROPIC_API_KEY in credential precedence, so the request still goes through the governed proxy and no authorization gap exists; the team should enforce proxy routing with an org-wide managed setting rather than relying on individual shell discipline.\" [checked] [disabled]:\n  - text: A)\n  - code: ANTHROPIC_AUTH_TOKEN\n  - text: outranks\n  - code: ANTHROPIC_API_KEY\n  - text: in credential precedence, so the request still goes through the governed proxy and no authorization gap exists; the team should enforce proxy routing with an org-wide managed setting rather than relying on individual shell discipline.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - text: Correct. Official Claude Code credential precedence lists cloud provider credentials first, then\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: \", then\"\n    - code: ANTHROPIC_API_KEY\n    - text: . Because\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: outranks\n    - code: ANTHROPIC_API_KEY\n    - text: \", the bearer token is used and the developer's key is ignored, so traffic still goes through the governed proxy. Best practice is to enforce the proxy path with an org-wide managed setting rather than relying on individual shell hygiene; this prevents accidental bypass if\"\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: is ever unset.\n  - radio \"B) ANTHROPIC_API_KEY is checked after ANTHROPIC_AUTH_TOKEN in precedence, so the proxy uses the bearer token and the API key is ignored; the team should add a shell check to unset ANTHROPIC_API_KEY when ANTHROPIC_AUTH_TOKEN is present.\" [disabled]\n  - text: B)\n  - code: ANTHROPIC_API_KEY\n  - text: is checked after\n  - code: ANTHROPIC_AUTH_TOKEN\n  - text: in precedence, so the proxy uses the bearer token and the API key is ignored; the team should add a shell check to unset\n  - code: ANTHROPIC_API_KEY\n  - text: when\n  - code: ANTHROPIC_AUTH_TOKEN\n  - text: is present.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - text: Incorrect. The precedence fact is correct—\n    - code: ANTHROPIC_API_KEY\n    - text: is checked after\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: —but adding a shell check to unset the API key when the token is present is unnecessary and fragile. Because\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: already outranks\n    - code: ANTHROPIC_API_KEY\n    - text: \", the key is ignored. Enterprise controls should be enforced through org-wide managed settings, not individual shell hygiene.\"\n  - radio \"C) Both variables get merged into a single request, sending the API key as an extra header beside the bearer token, so the proxy ignores it and authorization remains unchanged; the team can verify this by checking the proxy logs for only the expected bearer token.\" [disabled]\n  - generic: C) Both variables get merged into a single request, sending the API key as an extra header beside the bearer token, so the proxy ignores it and authorization remains unchanged; the team can verify this by checking the proxy logs for only the expected bearer token.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - text: Incorrect. Anthropic does not merge both credentials into one request. Claude Code uses credential precedence to select a single credential. When\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: is present, it is used and\n    - code: ANTHROPIC_API_KEY\n    - text: is ignored.\n  - radio \"D) ANTHROPIC_API_KEY ranks above ANTHROPIC_AUTH_TOKEN, so the session bypasses the gateway proxy's rotation and audit controls entirely; enforce the gateway path with an org-wide managed setting instead of developer discipline.\" [disabled]\n  - text: D)\n  - code: ANTHROPIC_API_KEY\n  - text: ranks above\n  - code: ANTHROPIC_AUTH_TOKEN\n  - text: \", so the session bypasses the gateway proxy's rotation and audit controls entirely; enforce the gateway path with an org-wide managed setting instead of developer discipline.\"\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded] [active]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - text: Incorrect. Official authentication precedence in Claude Code places\n    - code: ANTHROPIC_AUTH_TOKEN\n    - text: above\n    - code: ANTHROPIC_API_KEY\n    - text: \", not the reverse. An org-wide managed setting is a reasonable remediation, but the stated consequence of gateway bypass is false because the bearer token takes precedence.\"\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:01:18.725Z",
  "fingerprint": "0b7b3ccc7401196186ae7b8ee19d21c8e9a8b846475bfe9def1540e076ea5506"
}
