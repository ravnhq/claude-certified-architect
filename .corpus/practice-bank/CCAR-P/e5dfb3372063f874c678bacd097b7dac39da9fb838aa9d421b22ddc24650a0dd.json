{
  "exam": "CCAR-P",
  "question": "A developer builds an agent that calls the Messages API repeatedly. The system prompt contains roughly 2,000 tokens of stable tool-usage guidelines followed by a line, \"Current session timestamp: <ISO8601>\", that changes on every call. They place the cache_control breakpoint on that final timestamp block, hoping to cache the guidelines above it. After many calls, usage.cache_read_input_tokens stays at 0 every time. What is the most likely cause and fix?",
  "options": [
    "A) Caching requires the tools array to carry a cache_control block; without it, the system array is never cached, so the developer should add one to the tools array.",
    "B) Ephemeral cache entries are designed to expire immediately after creation, so cache keys are never reused; the solution is to use a fixed timestamp value instead.",
    "C) The Messages API only caches user-role content, so a cache_control marker in the system array is ignored; moving the guidelines to a user message resolves this.",
    "D) The breakpoint on the timestamp block changes the cache prefix every call because the text varies; moving it after the stable guidelines fixes the issue."
  ],
  "correct_answers": [
    "D) The breakpoint on the timestamp block changes the cache prefix every call because the text varies; moving it after the stable guidelines fixes the issue."
  ],
  "explanations": [
    "Incorrect. Each section (system, messages, tools) can have its own cache_control breakpoint independently. Caching the system array does not require the tools array to also have a cache_control block.",
    "Incorrect. Ephemeral caches have a default time-to-live of 5 minutes (extendable to 1 hour) and do not expire immediately. Repeated calls within the TTL window can produce cache hits; the issue is not immediate expiration but incorrect breakpoint placement.",
    "Incorrect. System messages, tool definitions, and message content are all cacheable. The API does not restrict caching to user-role content, so a cache_control marker in the system array is not ignored.",
    "Correct. The cache system computes a hash of all content preceding the breakpoint. Since the breakpoint is placed on the timestamp block, which changes on every call, the hashed prefix differs each time, preventing cache hits. Moving the breakpoint to just after the stable guidelines ensures a consistent prefix that can be reused."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: the practice bank — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Architect – Professional (CCAR-P)\n- button \"0 / 11 correct (0%) View by domain\":\n  - generic: 0 / 11 correct (0%)\n  - generic: View by domain\n- progressbar\n- generic: Question 12 of 60\n- generic: \"Subdomain 2.5: Implement prompt reuse strategies\"\n- 'heading \"A developer builds an agent that calls the Messages API repeatedly. The system prompt contains roughly 2,000 tokens of stable tool-usage guidelines followed by a line, \\\"Current session timestamp: <ISO8601>\\\", that changes on every call. They place the cache_control breakpoint on that final timestamp block, hoping to cache the guidelines above it. After many calls, usage.cache_read_input_tokens stays at 0 every time. What is the most likely cause and fix?\" [level=3]'\n- paragraph: Incorrect\n- paragraph: Review the explanations to understand the correct answer.\n- radiogroup:\n  - radio \"A) Caching requires the tools array to carry a cache_control block; without it, the system array is never cached, so the developer should add one to the tools array.\" [checked] [disabled]:\n  - generic: A) Caching requires the tools array to carry a cache_control block; without it, the system array is never cached, so the developer should add one to the tools array.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Each section (system, messages, tools) can have its own cache_control breakpoint independently. Caching the system array does not require the tools array to also have a cache_control block.\n  - radio \"B) Ephemeral cache entries are designed to expire immediately after creation, so cache keys are never reused; the solution is to use a fixed timestamp value instead.\" [disabled]\n  - generic: B) Ephemeral cache entries are designed to expire immediately after creation, so cache keys are never reused; the solution is to use a fixed timestamp value instead.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Ephemeral caches have a default time-to-live of 5 minutes (extendable to 1 hour) and do not expire immediately. Repeated calls within the TTL window can produce cache hits; the issue is not immediate expiration but incorrect breakpoint placement.\n  - radio \"C) The Messages API only caches user-role content, so a cache_control marker in the system array is ignored; moving the guidelines to a user message resolves this.\" [disabled]\n  - generic: C) The Messages API only caches user-role content, so a cache_control marker in the system array is ignored; moving the guidelines to a user message resolves this.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. System messages, tool definitions, and message content are all cacheable. The API does not restrict caching to user-role content, so a cache_control marker in the system array is not ignored.\n  - radio \"D) The breakpoint on the timestamp block changes the cache prefix every call because the text varies; moving it after the stable guidelines fixes the issue.\" [disabled]\n  - generic: D) The breakpoint on the timestamp block changes the cache prefix every call because the text varies; moving it after the stable guidelines fixes the issue.\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded] [active]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Correct. The cache system computes a hash of all content preceding the breakpoint. Since the breakpoint is placed on the timestamp block, which changes on every call, the hashed prefix differs each time, preventing cache hits. Moving the breakpoint to just after the stable guidelines ensures a consistent prefix that can be reused.\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:37:15.657Z",
  "fingerprint": "e5dfb3372063f874c678bacd097b7dac39da9fb838aa9d421b22ddc24650a0dd"
}
