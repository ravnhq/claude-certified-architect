{
  "exam": "CCDV-F",
  "question": "Two different endpoints in the same service begin failing: one returns HTTP 401 with authentication_error, and the other returns HTTP 403 with permission_error. Both endpoints use the same API key. What is the most likely explanation for why one endpoint fails with each code?",
  "options": [
    "A) The 401 endpoint's client fails to attach the key on that call, producing an authentication error, while the 403 endpoint accepts the key but lacks permission for its resource",
    "B) Both errors stem from the same key being close to expiring, and the 403 will resolve on its own once the key's expiration date is extended in the Console",
    "C) The 401 endpoint hit its requests-per-minute limit, while the 403 endpoint hit its input-tokens-per-minute limit on the same key, both of which the client mistakenly logs as authentication failures",
    "D) The 401 indicates the request body was malformed for that specific call, while the 403 indicates the same malformed request body on the other endpoint using that key"
  ],
  "correct_answers": [
    "A) The 401 endpoint's client fails to attach the key on that call, producing an authentication error, while the 403 endpoint accepts the key but lacks permission for its resource"
  ],
  "explanations": [
    "Correct. A 401 authentication_error means there was an issue with how the key reached the server on that specific call — for example a client bug that omits or malforms the key header on that one code path — which is a property of that request, not proof the stored key value itself is bad. A 403 permission_error only appears once the key has been received and recognized; it means that key lacks permission for that particular resource. Because the second endpoint gets far enough to check permissions on the same key, the key itself is fine, so the first endpoint's failure has to be in how that endpoint sends it, not in the key's own state.",
    "Incorrect. If the key were actually close to expiring or already expired, every endpoint using it would return 401 authentication_error, not a mix of 401 and 403; a 403 permission_error only appears once the key has already been accepted as valid, so seeing 403 at the other endpoint shows the key is not the problem there.",
    "Incorrect. Hitting a requests-per-minute or input-tokens-per-minute limit produces a 429 rate_limit_error, not 401 or 403, so neither observed failure matches what a rate limit actually returns.",
    "Incorrect. A malformed request body produces a 400 invalid_request_error, not 401 or 403, so this does not account for either observed failure."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: the practice bank — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Developer – Foundations (CCDV-F)\n- button \"9 / 38 correct (24%) View by domain\":\n  - generic: 9 / 38 correct (24%)\n  - generic: View by domain\n- progressbar\n- generic: Question 39 of 60\n- generic: \"Subdomain 4.1: Debugging and Error Handling\"\n- 'heading \"Two different endpoints in the same service begin failing: one returns HTTP 401 with authentication_error, and the other returns HTTP 403 with permission_error. Both endpoints use the same API key. What is the most likely explanation for why one endpoint fails with each code?\" [level=3]':\n  - text: \"Two different endpoints in the same service begin failing: one returns HTTP 401 with\"\n  - code: authentication_error\n  - text: \", and the other returns HTTP 403 with\"\n  - code: permission_error\n  - text: . Both endpoints use the same API key. What is the most likely explanation for why one endpoint fails with each code?\n- paragraph: Correct!\n- paragraph: Great job! Review the explanations below.\n- radiogroup:\n  - radio \"A) The 401 endpoint's client fails to attach the key on that call, producing an authentication error, while the 403 endpoint accepts the key but lacks permission for its resource\" [checked] [disabled]:\n  - generic: A) The 401 endpoint's client fails to attach the key on that call, producing an authentication error, while the 403 endpoint accepts the key but lacks permission for its resource\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Correct. A 401 authentication_error means there was an issue with how the key reached the server on that specific call — for example a client bug that omits or malforms the key header on that one code path — which is a property of that request, not proof the stored key value itself is bad. A 403 permission_error only appears once the key has been received and recognized; it means that key lacks permission for that particular resource. Because the second endpoint gets far enough to check permissions on the same key, the key itself is fine, so the first endpoint's failure has to be in how that endpoint sends it, not in the key's own state.\n  - radio \"B) Both errors stem from the same key being close to expiring, and the 403 will resolve on its own once the key's expiration date is extended in the Console\" [disabled]\n  - generic: B) Both errors stem from the same key being close to expiring, and the 403 will resolve on its own once the key's expiration date is extended in the Console\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. If the key were actually close to expiring or already expired, every endpoint using it would return 401 authentication_error, not a mix of 401 and 403; a 403 permission_error only appears once the key has already been accepted as valid, so seeing 403 at the other endpoint shows the key is not the problem there.\n  - radio \"C) The 401 endpoint hit its requests-per-minute limit, while the 403 endpoint hit its input-tokens-per-minute limit on the same key, both of which the client mistakenly logs as authentication failures\" [disabled]\n  - generic: C) The 401 endpoint hit its requests-per-minute limit, while the 403 endpoint hit its input-tokens-per-minute limit on the same key, both of which the client mistakenly logs as authentication failures\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Hitting a requests-per-minute or input-tokens-per-minute limit produces a 429 rate_limit_error, not 401 or 403, so neither observed failure matches what a rate limit actually returns.\n  - radio \"D) The 401 indicates the request body was malformed for that specific call, while the 403 indicates the same malformed request body on the other endpoint using that key\" [disabled]\n  - generic: D) The 401 indicates the request body was malformed for that specific call, while the 403 indicates the same malformed request body on the other endpoint using that key\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded] [active]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. A malformed request body produces a 400 invalid_request_error, not 401 or 403, so this does not account for either observed failure.\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:44:36.442Z",
  "fingerprint": "2f4b453ebeb973374f01b4b07ff6210967ca7f3be85728c17f6a37f601fd7a0f"
}
