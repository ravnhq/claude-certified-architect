{
  "exam": "CCAR-F",
  "question": "An architect asks Claude Code to find every place in a large monorepo that calls a function named parseInvoice, including calls inside a minified bundle that is gitignored but still needs to be checked. Which approach correctly locates all call sites?",
  "options": [
    "A) Run Glob with the pattern **/*.js to list every JavaScript file, then judge from file names alone which ones likely reference parseInvoice",
    "B) Run Grep across the repo for parseInvoice, then Grep the gitignored bundle's path directly, since a direct path is still searched",
    "C) Run Grep once with the multiline flag enabled, assuming multiline mode makes Grep search gitignored files as a side effect of that flag",
    "D) Run Glob with the pattern **/parseInvoice* to find files whose names contain the function, then treat that file list as the complete set of callers"
  ],
  "correct_answers": [
    "B) Run Grep across the repo for parseInvoice, then Grep the gitignored bundle's path directly, since a direct path is still searched"
  ],
  "explanations": [
    "Incorrect. Glob only matches file paths by name pattern; it cannot see whether a file's contents reference parseInvoice, so this cannot identify call sites.",
    "Correct. Grep respects .gitignore and skips gitignored files by default, but passing a gitignored file's path directly still searches it, so a normal repo-wide Grep plus a targeted Grep on the bundle path covers both tracked and gitignored call sites.",
    "Incorrect. The multiline flag changes whether a pattern can match across line boundaries; it has no effect on whether gitignored files are included in the search.",
    "Incorrect. Globbing for files named after the function only finds a file that happens to share the name, not the files where the function is called, so real call sites in other files are missed."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: the practice bank — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Architect – Foundations (CCAR-F)\n- button \"18 / 56 correct (32%) View by domain\":\n  - generic: 18 / 56 correct (32%)\n  - generic: View by domain\n- progressbar\n- generic: Question 57 of 60\n- generic: \"Subdomain 2.5: Select and apply built-in tools (Read, Write, Edit, Bash, Grep, Glob) effectively\"\n- heading \"An architect asks Claude Code to find every place in a large monorepo that calls a function named parseInvoice, including calls inside a minified bundle that is gitignored but still needs to be checked. Which approach correctly locates all call sites?\" [level=3]:\n  - text: An architect asks Claude Code to find every place in a large monorepo that calls a function named\n  - code: parseInvoice\n  - text: \", including calls inside a minified bundle that is gitignored but still needs to be checked. Which approach correctly locates all call sites?\"\n- paragraph: Incorrect\n- paragraph: Review the explanations to understand the correct answer.\n- radiogroup:\n  - radio \"A) Run Glob with the pattern **/*.js to list every JavaScript file, then judge from file names alone which ones likely reference parseInvoice\" [checked] [disabled]:\n  - generic: A) Run Glob with the pattern **/*.js to list every JavaScript file, then judge from file names alone which ones likely reference parseInvoice\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Glob only matches file paths by name pattern; it cannot see whether a file's contents reference parseInvoice, so this cannot identify call sites.\n  - radio \"B) Run Grep across the repo for parseInvoice, then Grep the gitignored bundle's path directly, since a direct path is still searched\" [disabled]\n  - generic: B) Run Grep across the repo for parseInvoice, then Grep the gitignored bundle's path directly, since a direct path is still searched\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Correct. Grep respects .gitignore and skips gitignored files by default, but passing a gitignored file's path directly still searches it, so a normal repo-wide Grep plus a targeted Grep on the bundle path covers both tracked and gitignored call sites.\n  - radio \"C) Run Grep once with the multiline flag enabled, assuming multiline mode makes Grep search gitignored files as a side effect of that flag\" [disabled]\n  - generic: C) Run Grep once with the multiline flag enabled, assuming multiline mode makes Grep search gitignored files as a side effect of that flag\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. The multiline flag changes whether a pattern can match across line boundaries; it has no effect on whether gitignored files are included in the search.\n  - radio \"D) Run Glob with the pattern **/parseInvoice* to find files whose names contain the function, then treat that file list as the complete set of callers\" [disabled]\n  - generic: D) Run Glob with the pattern **/parseInvoice* to find files whose names contain the function, then treat that file list as the complete set of callers\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded] [active]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. Globbing for files named after the function only finds a file that happens to share the name, not the files where the function is called, so real call sites in other files are missed.\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:45:38.787Z",
  "fingerprint": "4cd7401ab057f1b82e7d23e55b8a6d7b07991f1139dee75cf6bcd05a59207ff1"
}
