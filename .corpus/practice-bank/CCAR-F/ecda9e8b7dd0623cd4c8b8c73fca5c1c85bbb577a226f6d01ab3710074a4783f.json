{
  "exam": "CCAR-F",
  "question": "A maintainer writes a project CLAUDE.md that includes the line See @README for project overview and @package.json for available npm commands. They also want to mention the file @docs/legacy-notes.md in a sentence purely as a pointer for humans, without Claude actually importing and loading its contents into context. How should they write that second reference?",
  "options": [
    "A) Wrap the path in backticks so it becomes a Markdown code span, since import parsing skips code spans and treats the reference as literal text",
    "B) Put a space between the @ and the path like @ docs/legacy-notes.md, since only paths immediately adjacent to @ with no space are treated as imports",
    "C) Prefix it with a double @@docs/legacy-notes.md, since a doubled @ symbol tells the parser to render the path without importing it",
    "D) Place it inside an HTML comment <!-- @docs/legacy-notes.md -->, since HTML comments are rendered as plain visible text but excluded from imports"
  ],
  "correct_answers": [
    "A) Wrap the path in backticks so it becomes a Markdown code span, since import parsing skips code spans and treats the reference as literal text"
  ],
  "explanations": [
    "Correct. Claude Code's memory documentation states that import parsing skips Markdown code spans and fenced code blocks, and that to mention a path without importing it you wrap it in backticks, which keeps the text literal while the same path outside backticks imports the file. A code span therefore leaves the reference visible to human readers of the file and out of the import expansion.",
    "Incorrect. The memory documentation defines no whitespace rule for suppressing an import; the escape it specifies is a Markdown code span. Separating the at-sign from the path also stops the text being a file reference at all, so the sentence no longer points a human reader at the file under discussion.",
    "Incorrect. The memory documentation describes exactly one way to keep a path literal in a CLAUDE.md file — wrapping it in backticks so it sits inside a Markdown code span — and never mentions a doubled at-sign. Nothing in the documented import syntax gives @@ an escaping meaning, so this form is not a supported way to suppress the import.",
    "Incorrect. HTML comments are not visible in rendered Markdown, so they cannot serve as the visible pointer the maintainer wants. The memory documentation also states that block-level HTML comments in CLAUDE.md files are stripped before the content is injected into Claude's context, which hides the note entirely rather than displaying it while skipping the import."
  ],
  "snapshot": "- region \"Notifications (F8)\":\n  - list\n- region \"Notifications alt+T\"\n- alert: the practice bank — Free Cloud, Data & AI Certification Practice Questions\n- button \"Exit Quiz\":\n  - text: Exit Quiz\n- button \"Toggle theme\":\n  - generic: Toggle theme\n- generic: Anthropic Claude Certified Architect – Foundations (CCAR-F)\n- button \"11 / 40 correct (28%) View by domain\":\n  - generic: 11 / 40 correct (28%)\n  - generic: View by domain\n- progressbar\n- generic: Question 41 of 60\n- generic: \"Subdomain 3.1: Configure CLAUDE.md files with appropriate hierarchy, scoping, and modular organization\"\n- heading \"A maintainer writes a project CLAUDE.md that includes the line See @README for project overview and @package.json for available npm commands. They also want to mention the file @docs/legacy-notes.md in a sentence purely as a pointer for humans, without Claude actually importing and loading its contents into context. How should they write that second reference?\" [level=3]:\n  - text: A maintainer writes a project\n  - code: CLAUDE.md\n  - text: that includes the line\n  - code: See @README for project overview and @package.json for available npm commands.\n  - text: They also want to mention the file\n  - code: \"@docs/legacy-notes.md\"\n  - text: in a sentence purely as a pointer for humans, without Claude actually importing and loading its contents into context. How should they write that second reference?\n- paragraph: Correct!\n- paragraph: Great job! Review the explanations below.\n- radiogroup:\n  - radio \"A) Wrap the path in backticks so it becomes a Markdown code span, since import parsing skips code spans and treats the reference as literal text\" [checked] [disabled]:\n  - generic: A) Wrap the path in backticks so it becomes a Markdown code span, since import parsing skips code spans and treats the reference as literal text\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Correct. Claude Code's memory documentation states that import parsing skips Markdown code spans and fenced code blocks, and that to mention a path without importing it you wrap it in backticks, which keeps the text literal while the same path outside backticks imports the file. A code span therefore leaves the reference visible to human readers of the file and out of the import expansion.\n  - radio \"B) Put a space between the @ and the path like @ docs/legacy-notes.md, since only paths immediately adjacent to @ with no space are treated as imports\" [disabled]\n  - text: B) Put a space between the\n  - code: \"@\"\n  - text: and the path like\n  - code: \"@ docs/legacy-notes.md\"\n  - text: \", since only paths immediately adjacent to\"\n  - code: \"@\"\n  - text: with no space are treated as imports\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. The memory documentation defines no whitespace rule for suppressing an import; the escape it specifies is a Markdown code span. Separating the at-sign from the path also stops the text being a file reference at all, so the sentence no longer points a human reader at the file under discussion.\n  - radio \"C) Prefix it with a double @@docs/legacy-notes.md, since a doubled @ symbol tells the parser to render the path without importing it\" [disabled]\n  - text: C) Prefix it with a double\n  - code: \"@@docs/legacy-notes.md\"\n  - text: \", since a doubled\"\n  - code: \"@\"\n  - text: symbol tells the parser to render the path without importing it\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - text: Incorrect. The memory documentation describes exactly one way to keep a path literal in a CLAUDE.md file — wrapping it in backticks so it sits inside a Markdown code span — and never mentions a doubled at-sign. Nothing in the documented import syntax gives\n    - code: \"@@\"\n    - text: an escaping meaning, so this form is not a supported way to suppress the import.\n  - radio \"D) Place it inside an HTML comment <!-- @docs/legacy-notes.md -->, since HTML comments are rendered as plain visible text but excluded from imports\" [disabled]\n  - text: D) Place it inside an HTML comment\n  - code: <!-- @docs/legacy-notes.md -->\n  - text: \", since HTML comments are rendered as plain visible text but excluded from imports\"\n  - heading \"Explanation\" [level=3]:\n    - button \"Explanation\" [expanded] [active]:\n      - generic: Explanation\n  - region \"Explanation\":\n    - generic: Incorrect. HTML comments are not visible in rendered Markdown, so they cannot serve as the visible pointer the maintainer wants. The memory documentation also states that block-level HTML comments in CLAUDE.md files are stripped before the content is injected into Claude's context, which hides the note entirely rather than displaying it while skipping the import.\n- button \"Save question for later\":\n  - generic: Save\n- button \"Add my note\":\n  - generic: Note\n- button \"Report\":\n  - generic: Report\n- button \"Copy study prompt\":\n- button \"Next Question\"",
  "captured_at": "2026-09-14T16:12:40.139Z",
  "fingerprint": "ecda9e8b7dd0623cd4c8b8c73fca5c1c85bbb577a226f6d01ab3710074a4783f"
}
