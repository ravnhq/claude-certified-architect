.DS_Store
.claude/*
!.claude/skills/
.claude/skills/*.zip
.claude/skills/book-to-skill

node_modules/
package-lock.json
package.json

# generated by scripts/build-pages.mjs and committed only as a Pages artifact
docs/index.html
docs/.nojekyll
docs/search-index.json
docs/score-report.html
docs/score-report.js
docs/score-report-data.json
docs/guides/
docs/practical/
docs/cheatsheet/
docs/preflight/
docs/pdf/
docs/vendor/
__pycache__/

# local corpus — Skilljar course material is partner-gated, never published;
# certsafari/ is a tracked exception (see scripts/certsafari-collection.md)
.corpus/

# vendored agent tooling — reinstall from skills-lock.json, do not commit
.agents/

.firecrawl/

# Impeccable live mode: keep the project config, drop the per-run state
# (helper token and pid, absolute local paths, session journals).
.impeccable/live/server.json
.impeccable/live/roots.json
.impeccable/live/inject-journal.json
.impeccable/live/sessions/

# Lighthouse CI scratch: the served-site symlink and the collected reports
.lhci-site/
.lighthouseci/
